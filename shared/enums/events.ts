enum lobbyEvents {
  joinLobby = "join-lobby",
  play = "play",
}

enum gameEvents {
  connected = "connected",
  start = "start",
  notReady = "not-ready",
  moveFinished = "move-finished",
  updateBoard = "update-board",
}